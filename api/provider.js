export async function createProviderOrder(order) {
// Provider integration placeholder.
// We will connect a verified provider/API here later.

return {
success: false,
status: "PENDING_PROVIDER",
message: "No provider connected yet.",
order: order
};
}
